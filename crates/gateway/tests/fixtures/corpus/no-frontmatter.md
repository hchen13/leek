# Bare File

A markdown file with no YAML frontmatter at all. The loader should
still ingest it; the title falls back to the first `# heading`, and
the frontmatter map is empty.

Body mentions: alpha, beta, gamma — useful as a tokenizer probe.
